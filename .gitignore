# Rust
/target/
**/target/
# NOTE: Cargo.lock is intentionally NOT ignored — this is a workspace POC and
# the lockfile should be committed for reproducible builds.

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml
**/*.rs.bk

# OS
Thumbs.db
.DS_Store

# Build / runtime artifacts (generated by the pipeline)
*.nse
*.safetensors
*.pt
*.pth
data/sweep/

# LaTeX / paper build
/paper/_build/
*.aux
*.log
*.out
*.toc
*.synctex.gz

# NOTE: .zcode/ is intentionally NOT ignored — local session/plan data is
# tracked in this repo (see .zcode/plans/).
